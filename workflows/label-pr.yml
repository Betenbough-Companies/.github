name: Label PR by Author

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  team-labeler:
    runs-on: ubuntu-latest
    steps:
      - uses: JulienKode/team-labeler-action@v2.0.1
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"